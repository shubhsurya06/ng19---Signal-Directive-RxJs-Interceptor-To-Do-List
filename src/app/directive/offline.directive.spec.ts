import { OfflineDirective } from './offline.directive';

describe('OfflineDirective', () => {
  it('should create an instance', () => {
    const directive = new OfflineDirective();
    expect(directive).toBeTruthy();
  });
});
